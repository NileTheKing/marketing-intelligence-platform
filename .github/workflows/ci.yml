name: CI

on:
  pull_request:
  push:
    branches: ["main"]

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "temurin"
          cache: "gradle"

      - name: Make Gradle wrappers executable
        run: |
          chmod +x core-service/gradlew
          chmod +x entry-service/gradlew

      - name: Test core-service
        working-directory: core-service
        run: ./gradlew test --no-daemon

      - name: Verify core integration suites executed
        run: |
          bash scripts/ci/assert-test-suite-ran.sh \
            core-service/build/test-results/test/TEST-com.axon.core_service.service.PurchaseFlowIntegrationTest.xml \
            core-service/build/test-results/test/TEST-com.axon.core_service.controller.CampaignActivityHttpDbIntegrationTest.xml

      - name: Test entry-service
        working-directory: entry-service
        run: ./gradlew test --no-daemon

      - name: Verify entry integration suite executed
        run: |
          bash scripts/ci/assert-test-suite-ran.sh \
            entry-service/build/test-results/test/TEST-com.axon.entry_service.service.EntryReservationServiceRedisIntegrationTest.xml

      - name: Validate Docker Compose config
        run: docker compose -f compose.app.yml config --quiet

      - name: Build application images
        run: docker compose -f compose.app.yml build core-service entry-service
