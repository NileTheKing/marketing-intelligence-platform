services:
  entry-service:
    environment:
      SPRING_PROFILES_ACTIVE: ${ENTRY_SPRING_PROFILES_ACTIVE:-diagnostic}
      AXON_DIAGNOSTIC_ENTRY_SLOW_THRESHOLD_MS: ${AXON_DIAGNOSTIC_ENTRY_SLOW_THRESHOLD_MS:-100}
      AXON_DIAGNOSTIC_BACKEND_EVENT_PUBLISH_ENABLED: ${AXON_DIAGNOSTIC_BACKEND_EVENT_PUBLISH_ENABLED:-true}
